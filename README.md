## answer two the questin no.1
let, var, and const difference between var is a global scope let and const is block scope.var and let allow value to be changed but const dosen't changed the value. var variable can be re-asign with the same name but cannot be const and let.

## answer two the questin no.2
The spread operator can create a new copy of an array or object and spread out its values.

## answer two the questin no.3
map(), filter(), and forEach() difference between map() Returns a new array by operating on the elements of the array, filter() Selects some elements according to the condition and returns a new array and forEach() Does't returns a new array by operating on the elements of the array.

## answer two the questin no.4
Arrow functions are a concise, ES6-introduced syntax for writing JavaScript functions.Arrow functions are ideal for small, one-line functions. You don't need to write a return for writing one line of code, but you do need to write a return for writing many lines of code.

## answer two the questin no.5
Template literals are a way to create strings in JavaScript using backticks, allowing embedded expressions with ${} and supporting multi-line strings.

